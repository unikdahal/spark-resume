<?xml version="1.0" encoding="UTF-8"?>
<!--
  A second, independent ExchangeStore implementation (design doc sec 15's conformance-suite
  deliverable) : NOT built to prove out a real disaggregated shuffle service (spark-resume-celeborn
  already does that). Its purpose is narrower and different : is ExchangeStoreContract actually
  satisfiable, completely, by someone who did not write it, using only the published testkit?
  Real files, real bytes read back off disk, zero external infrastructure needed : see
  FsExchangeStore's own doc comment for the on-disk layout and why reattach here (unlike Celeborn's)
  is the first REAL exerciser of the contract's reattach-success path other than the reference
  InMemoryExchangeStore.

  Depends on spark-resume-api ONLY : no Spark dependency, no external service client, nothing but
  java.nio.file.
-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.apache.spark.resume</groupId>
    <artifactId>spark-resume-parent</artifactId>
    <version>0.1.0-SNAPSHOT</version>
  </parent>

  <artifactId>spark-resume-fs_2.12</artifactId>
  <packaging>jar</packaging>

  <dependencies>
    <dependency>
      <groupId>org.scala-lang</groupId>
      <artifactId>scala-library</artifactId>
    </dependency>
    <dependency>
      <groupId>org.apache.spark.resume</groupId>
      <artifactId>spark-resume-api_2.12</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>org.scalatest</groupId>
      <artifactId>scalatest_${scala.binary.version}</artifactId>
      <scope>test</scope>
    </dependency>
    <!-- ExchangeStoreContract, the conformance testkit this module exists to stress-test as a
         second, independent implementer : see docs/DESIGN.md sec 12. -->
    <dependency>
      <groupId>org.apache.spark.resume</groupId>
      <artifactId>spark-resume-api_2.12</artifactId>
      <version>${project.version}</version>
      <type>test-jar</type>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <sourceDirectory>src/main/scala</sourceDirectory>
    <testSourceDirectory>src/test/scala</testSourceDirectory>
    <plugins>
      <plugin>
        <groupId>net.alchim31.maven</groupId>
        <artifactId>scala-maven-plugin</artifactId>
      </plugin>
      <plugin>
        <groupId>org.scalatest</groupId>
        <artifactId>scalatest-maven-plugin</artifactId>
      </plugin>
    </plugins>
  </build>
</project>
